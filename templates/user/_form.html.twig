{{ form_start(form) }}

    {{ form_row(form.email, { 'disabled':'disabled' }) }}
    {{ form_row(form.firstName) }}
    {{ form_row(form.lastName) }}
    {{ form_row(form.birthDate) }}
    {{ form_row(form.phone) }}
    {{ form_row(form.licenceNumber) }}
    {# {{ form_row(form.roles, { 'attr': {'class': 'hidden-row'}}) }} #}
    <div class="row">
    <div class="ml-auto">
        <button class="btn btn-warning mb-2 mr-3">Sauvegarder <i class="far fa-save"></i></button>
    </div>
    </div>
{{ form_end(form, {'render_rest': false}) }}
